/**
 * Public endpoint entrypoint for the AI composer stream route.
 *
 * The implementation lives in `./custom-handler/handler.ts` so endpoint
 * helpers, types, and stream utilities can stay focused and documented.
 */
export { customEndpointHandler } from './custom-handler/handler';
